#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Aim: To get a random number and compare the last digit with 5
 * Return: 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
