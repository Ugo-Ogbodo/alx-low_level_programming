#include "main.h"

/**
 * rev_string - reverses a string
 *
 * @s: string parameter input
 *
 * Return: nothing
 */

void rev_string(char *s)
{
	int l, i, len;
	char cool;

	while (s[i] != '\0')
	{
		cool[i] = s[i];
		i++;
	}
	len = i;

	while (j <= len - l)
	{
		s[i] = cool[i - l];
		j++;
		i--;
	}
}

